export async function getStatuses(body: unknown) {
  // TODO: calculate days remaining per entry, categorize expired/soon/fresh
}

export async function resetTimer(id: string | undefined) {
  // TODO: reset expiry date for the given entry
}
